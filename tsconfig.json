{
  "compilerOptions": {
    "target": "ESNext",  // 컴파일된 JavaScript의 ECMAScript 버전을 최신 버전으로 설정합니다.
    "module": "ESNext",  // 모듈 코드 생성을 지정합니다. 'ESNext'는 최신 모듈 시스템을 사용합니다.
    "sourceMap": true,  // 소스 맵을 생성하여, 디버깅 시 TypeScript 파일과 매핑되게 합니다.
    "strict": true,  // 모든 엄격한 타입-체킹 옵션을 활성화합니다.
    "esModuleInterop": true,  // CommonJS와 ES 모듈 간의 상호 운용성을 허용합니다.
    "skipLibCheck": true,  // 선언 파일의 타입 체크를 건너뜁니다.
    "forceConsistentCasingInFileNames": true,  // 파일 이름의 대소문자 일관성을 강제합니다.
    "moduleResolution": "node",  // Node.js 스타일로 모듈 해석을 설정합니다.
    "resolveJsonModule": true,  // JSON 모듈을 포함할 수 있게 합니다.
    "outDir": "./dist",  // 컴파일된 파일들을 저장할 디렉터리를 설정합니다.
    "baseUrl": ".",  // 비상대적 모듈 이름을 해석하는 기준 디렉터리를 설정합니다.
    "allowImportingTsExtensions": true,  // .ts 확장자를 명시적으로 가진 파일을 import할 수 있게 합니다.
    "noEmit": true  // 출력 파일을 생성하지 않습니다 (타입 체크만 수행).
  },
  "include": ["src/**/*.ts"],  // 컴파일 대상에 포함될 파일 패턴을 지정합니다.
  "exclude": ["node_modules", "**/*.spec.ts"],  // 컴파일 대상에서 제외할 파일 또는 폴더를 지정합니다.
  "ts-node": {
    "esm": true  // ts-node에서 ECMAScript 모듈을 사용하도록 설정합니다.
  }
}
